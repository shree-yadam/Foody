<nav>
  <div class="logo">
    <a href="/">
      <img class="brand-logo" src="/img/Foody_logo.png">
      Foody
    </a>
  </div>
  <!-- TBD: replace bottom div with partials with if statement -->
  <% if (customerId) { %>
    <%- include('./_navbarLoggedIn') %>
  <% } else { %>
    <%- include('./_navbarLoggedOut') %>
  <% } %>
</nav>
